{
  "$schema": "../schemas/model_config_schema.json",
  "general": {
    "device": "cuda",
    "checkpoint_name": "example"
  },
  "training_process": {
    "dataset": "GTO",
    "optimizer": "AdamW",
    "num_epochs": 2,
    "warm_start": false
  }
}
