{
  "$schema": "../schemas/model_config_schemas.json",
  "general": {
    "device": "cuda",
    "checkpoint_name": "example"
  },
  "training_process": {
    "dataset": "GTO",
    "optimizer": "AdamW",
    "num_epochs": 8,
    "warm_start": false,
    "learning_rate": 1e-3
  }
}
